#include "epollManager"

class epollManager
{
private:
    /* data */
public:
    epollManager(/* args */);
    ~epollManager();
};

epollManager::epollManager(/* args */)
{
}

epollManager::~epollManager()
{
}
